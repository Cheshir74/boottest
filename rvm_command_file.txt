source /usr/local/rvm/scripts/rvm
source /home/cheshir/.rvm/scripts/rvm
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*
rvm gemset list
rvm gemset use site_port
curl -sSL https://get.rvm.io | bash -s stable --ruby
source /home/cheshir/.rvm/scripts/rvm
rvm version
rvm get stable --autolibs=enable
usermod -a -G rvm root
rvm list known
rvm install ruby-2.6.3
source /home/cheshir/.rvm/scripts/rvm
export PATH="$PATH:$HOME/.rvm/bin"
rvm
rvm --version
rvm --list
rvm list
rvm use --default 2.6.3
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
rvm
rvm use --default 2.6.5
rvm install "ruby-2.6.5"
rvm use --default 2.6.5
rvm use --default 2.6.5
rvm use --site_port 2.6.5
rvm help
rvm list
rvm list known
rvm gemset
rvm use --default 2.6.5@site_port
rvm use 2.6.5@site_port
rvm use 2.6.5@site_port --create
